# The midnight intent builder's gate. The TypeScript package builds intents and
# submits them through a funding wallet. Its deterministic suite runs the real
# circuit and local HTTP boundary without requiring a full Midnight stack.
#
# The Rust half of the seam (`chain-midnight`'s IntentGen spawning `dist/main.js`)
# is not on this branch yet; its `intent_seam` differential test and the Rust
# toolchain steps that run it return in the PR that restores the Rust client, and
# the `chain-midnight` path filter below is kept so that PR only has to re-add
# the steps, not the triggers.
#
# The live end-to-end path (boot the dockerized stack, deploy a singleton,
# balance/prove/submit, confirm on chain) is deliberately NOT automated here.
name: Midnight Publisher TS

on:
  push:
    branches:
      - develop
    paths:
      - ".github/workflows/midnight-publisher-ts.yml"
      - "chain-signatures/midnight-publisher-ts/**"
      - "chain-signatures/chain-midnight/**"
  pull_request:
  merge_group:

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: chain-signatures/midnight-publisher-ts
    steps:
      - uses: actions/checkout@v6

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: 'npm'
          cache-dependency-path: chain-signatures/midnight-publisher-ts/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        run: npm run format:check

      - name: Lint TypeScript
        run: npm run lint

      - name: Typecheck TypeScript
        run: npm run typecheck

      # `npm test` runs the package's pretest build because the child-process
      # tests execute the emitted `dist/main.js` entry point.
      - name: Build and test TypeScript publisher
        run: npm test
