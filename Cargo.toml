[workspace]
members = [
    "mpc-recovery",
    "contract",
    "node",
    "integration-tests",
    "load-tests",
    "keys",
    "kdf",
    "test-oidc-provider",
]

[profile.flamegraph]
inherits = "release"
debug = true
